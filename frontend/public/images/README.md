# Landing page images

Add your image files in this folder using these names to replace the temporary remote defaults:

- `care-team.jpg`
- `patient-care.jpg`
- `medical-record.jpg`

The landing page at `/` displays them in a layered stack. JPG, PNG, and WebP files are supported; update the filenames in `src/pages/Landing.tsx` if you prefer different names.
